const nota1 = 10;
const nota2 = 7;
const nota3 = 2;
const media = (nota1, nota2, nota3 / 3);
const reprovado = "Reprovado";
const recuperacao = "Recuperação";
const aprovado = "Passou de semestre";

if (media >= 5 <=7) {
    console.log(recuperacao);
} 
    if else (media < 5) {
        console.log(reprovado);
    } else {
        console.log (aprovado);
    }